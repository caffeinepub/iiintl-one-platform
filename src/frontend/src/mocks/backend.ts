import type { backendInterface } from "../backend";
import {
  CampaignStatus,
  CampaignType,
  CredentialStatus,
  CredentialType,
  CrowdfundingCategory,
  CrowdfundingFundingModel,
  CrowdfundingStatus,
  DAOTokenTxType,
  EarningStatus,
  EarningType,
  ForumCategory,
  FSUTxType,
  MembershipTierLevel,
  NotificationType,
  OrgMemberRole,
  OrgStatus,
  ProposalStatus,
  ProposalType,
  RoyaltyPoolType,
  TenantStatus,
  TenantTier,
  ThreadStatus,
  TransactionType,
  UserRole,
  VoteChoice,
  VotingMechanism,
  WalletType,
} from "../backend";

const fakePrincipal = (text: string) => ({
  toText: () => text,
  toUint8Array: () => new Uint8Array(0),
  compareTo: () => "eq" as const,
  isAnonymous: () => false,
  toJSON: () => text,
  toString: () => text,
  // biome-ignore lint/suspicious/noExplicitAny: mock
} as any);

export const mockBackend: backendInterface = {
  _initializeAccessControl: async () => undefined,
  addBillingRecord: async () => "billing-1",
  addDebateComment: async () => ({ __kind__: "ok" as const, ok: BigInt(1) }),
  addTenantMember: async () => true,
  addToFSUPool: async () => undefined,
  addToRoyaltyPool: async () => true,
  addTransaction: async () => undefined,
  adminListAllCrowdfundingCampaigns: async () => [],
  adminListCrowdfundingPledges: async () => [],
  airdropToAllMembers: async () => ({ totalTokens: BigInt(100000), airdropped: BigInt(50) }),
  approveCredential: async () => true,
  approveCrowdfundingCampaign: async () => true,
  archiveCampaign: async () => true,
  archiveOrg: async () => true,
  archiveThread: async () => true,
  assignCallerUserRole: async () => undefined,
  burnDAOTokens: async () => ({ __kind__: "ok" as const, ok: BigInt(500) }),
  cancelProposal: async () => ({ __kind__: "ok" as const, ok: "cancelled" }),
  cancelTenant: async () => true,
  castVote: async () => ({ __kind__: "ok" as const, ok: "voted" }),
  checkAndExpireTrials: async () => ({ checked: BigInt(5), expired: BigInt(1) }),
  claimVotingReward: async () => ({ __kind__: "ok" as const, ok: BigInt(50) }),
  closeProposal: async () => ({ __kind__: "ok" as const, ok: "closed" }),
  createCampaign: async () => "campaign-1",
  createCrowdfundingCampaign: async () => "crowd-1",
  createOrg: async () => "org-1",
  createProposal: async () => ({ __kind__: "ok" as const, ok: BigInt(1) }),
  createRoyaltyPool: async () => "pool-1",
  createTenant: async () => "tenant-1",
  createThread: async () => BigInt(1),
  deactivateCommissionRate: async () => true,
  delegateVote: async () => ({ __kind__: "ok" as const, ok: "delegated" }),
  distributeFSU: async () => undefined,
  distributeRoyaltyPool: async () => true,
  editDebateComment: async () => ({ __kind__: "ok" as const, ok: "updated" }),
  enactProposal: async () => ({ __kind__: "ok" as const, ok: "enacted" }),
  finalizeCrowdfundingCampaign: async () => true,
  getCallerUserProfile: async () => ({
    displayName: "Demo Member",
    email: "demo@iiintl.org",
    bio: "Active civic member",
    avatarUrl: "",
  }),
  getCallerUserRole: async () => UserRole.user,
  getCampaign: async () => null,
  getCampaignPledges: async () => [],
  getCampaignProgress: async () => null,
  getCampaignSupporterCount: async () => null,
  getCommissionRate: async () => null,
  getCommissionRates: async () => [],
  getCredential: async (credId: string) => ({
    id: credId,
    credentialType: CredentialType.verifiedHuman,
    status: CredentialStatus.active,
    title: "Verified Human — Internet Identity",
    description: "Confirmed unique personhood verified via Internet Identity on the Internet Computer.",
    subject: fakePrincipal("rdmx6-jaaaa-aaaaa-aaadq-cai"),
    issuedBy: fakePrincipal("rrkah-fqaaa-aaaaa-aaaaq-cai"),
    metadata: "{}",
    isPublic: true,
    issuedAt: BigInt(Date.now() - 30 * 86400000) * BigInt(1000000),
    approvedAt: BigInt(Date.now() - 29 * 86400000) * BigInt(1000000),
    updatedAt: BigInt(Date.now()) * BigInt(1000000),
  }),
  getCrowdfundingCampaign: async () => null,
  getCrowdfundingConfig: async () => ({
    defaultFSUContributionBps: BigInt(200),
    milestoneAchievementBonusBps: BigInt(100),
    creatorFSUBonus: BigInt(500),
  }),
  getCrowdfundingPledge: async () => null,
  getDAOTokenStats: async () => ({
    treasuryBalance: BigInt(500000),
    totalHolders: BigInt(1247),
    totalSupply: BigInt(10000000),
  }),
  getDAOTransactionHistory: async () => [
    {
      id: BigInt(1),
      txType: DAOTokenTxType.airdrop,
      amount: BigInt(10000),
      from: undefined,
      to: fakePrincipal("rdmx6-jaaaa-aaaaa-aaadq-cai"),
      note: "Genesis airdrop — Ambassador tier",
      createdAt: BigInt(Date.now() - 7 * 86400000) * BigInt(1000000),
    },
    {
      id: BigInt(2),
      txType: DAOTokenTxType.votingReward,
      amount: BigInt(50),
      from: undefined,
      to: fakePrincipal("rdmx6-jaaaa-aaaaa-aaadq-cai"),
      note: "Voting reward — Proposal #1",
      createdAt: BigInt(Date.now() - 3 * 86400000) * BigInt(1000000),
    },
    {
      id: BigInt(3),
      txType: DAOTokenTxType.earned,
      amount: BigInt(250),
      from: undefined,
      to: fakePrincipal("rdmx6-jaaaa-aaaaa-aaadq-cai"),
      note: "MLM referral bonus",
      createdAt: BigInt(Date.now() - 1 * 86400000) * BigInt(1000000),
    },
  ],
  getDaoLeaderboard: async () => [
    {
      principal: fakePrincipal("rdmx6-jaaaa-aaaaa-aaadq-cai"),
      balance: BigInt(10300),
      createdAt: BigInt(Date.now() - 30 * 86400000) * BigInt(1000000),
      totalEarned: BigInt(12000),
      totalBurned: BigInt(1700),
      lastAirdropAt: BigInt(Date.now() - 7 * 86400000) * BigInt(1000000),
    },
    {
      principal: fakePrincipal("rrkah-fqaaa-aaaaa-aaaaq-cai"),
      balance: BigInt(8500),
      createdAt: BigInt(Date.now() - 25 * 86400000) * BigInt(1000000),
      totalEarned: BigInt(9000),
      totalBurned: BigInt(500),
      lastAirdropAt: BigInt(Date.now() - 7 * 86400000) * BigInt(1000000),
    },
    {
      principal: fakePrincipal("qaa6y-5yaaa-aaaaa-aaafa-cai"),
      balance: BigInt(5250),
      createdAt: BigInt(Date.now() - 20 * 86400000) * BigInt(1000000),
      totalEarned: BigInt(5500),
      totalBurned: BigInt(250),
      lastAirdropAt: BigInt(Date.now() - 7 * 86400000) * BigInt(1000000),
    },
    {
      principal: fakePrincipal("aaaaa-aa"),
      balance: BigInt(2500),
      createdAt: BigInt(Date.now() - 15 * 86400000) * BigInt(1000000),
      totalEarned: BigInt(2500),
      totalBurned: BigInt(0),
      lastAirdropAt: BigInt(Date.now() - 7 * 86400000) * BigInt(1000000),
    },
  ],
  getExpiringTrials: async () => [],
  getFSUPoolStatus: async () => ({
    totalOutstandingFSU: BigInt(1000000),
    valuePerUnitCents: BigInt(5),
    poolSizeUnits: BigInt(500000),
    nextDistributionLabel: "Q2 2026",
  }),
  getLinkedWallets: async () => [],
  getMemberEarnings: async () => [],
  getMemberTierRecord: async () => null,
  getMemberVote: async () => null,
  getMyCredentials: async () => [
    {
      id: "cred-001",
      credentialType: CredentialType.verifiedHuman,
      status: CredentialStatus.active,
      title: "Verified Human — Internet Identity",
      description: "Confirmed unique personhood via Internet Identity.",
      subject: fakePrincipal("rdmx6-jaaaa-aaaaa-aaadq-cai"),
      issuedBy: fakePrincipal("rrkah-fqaaa-aaaaa-aaaaq-cai"),
      metadata: "{}",
      isPublic: true,
      issuedAt: BigInt(Date.now() - 30 * 86400000) * BigInt(1000000),
      approvedAt: BigInt(Date.now() - 29 * 86400000) * BigInt(1000000),
      updatedAt: BigInt(Date.now()) * BigInt(1000000),
    },
    {
      id: "cred-003",
      credentialType: CredentialType.expertiseBadge,
      status: CredentialStatus.active,
      title: "International Law & Diplomacy Expert",
      description: "Peer-endorsed expertise in international law.",
      subject: fakePrincipal("rdmx6-jaaaa-aaaaa-aaadq-cai"),
      issuedBy: fakePrincipal("rrkah-fqaaa-aaaaa-aaaaq-cai"),
      metadata: '{"domain":"international-law"}',
      isPublic: false,
      issuedAt: BigInt(Date.now() - 14 * 86400000) * BigInt(1000000),
      approvedAt: BigInt(Date.now() - 13 * 86400000) * BigInt(1000000),
      updatedAt: BigInt(Date.now()) * BigInt(1000000),
    },
    {
      id: "cred-007",
      credentialType: CredentialType.activistCertification,
      status: CredentialStatus.pending,
      title: "Environmental Activist Certification",
      description: "Pending review for environmental activism campaigns.",
      subject: fakePrincipal("rdmx6-jaaaa-aaaaa-aaadq-cai"),
      issuedBy: fakePrincipal("rrkah-fqaaa-aaaaa-aaaaq-cai"),
      metadata: "{}",
      isPublic: false,
      issuedAt: BigInt(Date.now() - 3 * 86400000) * BigInt(1000000),
      updatedAt: BigInt(Date.now()) * BigInt(1000000),
    },
  ],
  getMyDAOBalance: async () => ({
    principal: fakePrincipal("rdmx6-jaaaa-aaaaa-aaadq-cai"),
    balance: BigInt(10300),
    createdAt: BigInt(Date.now() - 30 * 86400000) * BigInt(1000000),
    totalEarned: BigInt(12000),
    totalBurned: BigInt(1700),
    lastAirdropAt: BigInt(Date.now() - 7 * 86400000) * BigInt(1000000),
  }),
  getMyDelegation: async () => null,
  getMyDownline: async () => [],
  getMyEarnings: async () => [],
  getMyEarningsSummary: async () => ({
    eventCommission: BigInt(0),
    activityBonus: BigInt(0),
    totalPaid: BigInt(0),
    totalLifetime: BigInt(0),
    directReferral: BigInt(0),
    levelOverride: BigInt(0),
    totalPending: BigInt(0),
    royaltyPool: BigInt(0),
    finFracFran: BigInt(0),
  }),
  getMyFSURecord: async () => null,
  getMyFSUTransactions: async () => [],
  getMyNotifications: async () => [],
  getMyPledges: async () => [],
  getMyReferralCode: async () => "REF-DEMO",
  getMyRoyaltyDistributions: async () => [],
  getMyTenant: async () => null,
  getMyTierRecord: async () => ({
    principal: fakePrincipal("rdmx6-jaaaa-aaaaa-aaadq-cai"),
    referralCode: "REF-DEMO",
    joinedAt: BigInt(Date.now() - 90 * 86400000) * BigInt(1000000),
    tier: MembershipTierLevel.ambassador,
    upgradedAt: BigInt(Date.now() - 30 * 86400000) * BigInt(1000000),
  }),
  getMyUplineChain: async () => [],
  getOrg: async () => null,
  getOrgMembers: async () => [],
  getPreferredLanguage: async () => "en",
  getProposal: async () => null,
  getProposalComments: async () => [],
  getProposalCommentsPublic: async () => [],
  getProposalPublic: async () => null,
  getProposalSponsors: async () => [],
  getQuorumStatus: async () => ({
    totalVoters: BigInt(100),
    quorumPercent: BigInt(30),
    percentVoted: BigInt(45),
    quorumMet: true,
    votesCast: BigInt(45),
  }),
  getReplies: async () => [],
  getRoyaltyPool: async () => null,
  getTenant: async () => null,
  getTenantBranding: async () => null,
  getThread: async () => null,
  getTransactionHistory: async () => [],
  getTrialAutomationStatus: async () => ({
    lastCheck: BigInt(Date.now() - 3600000) * BigInt(1000000),
    nextCheckIn: "23 hours",
  }),
  getUserOrgs: async () => [],
  getUserProfile: async () => null,
  getVoteTally: async () => null,
  getVoteTallyPublic: async () => null,
  getVotesByProposal: async () => [],
  getWalletBalance: async () => 0,
  incrementThreadView: async () => true,
  initMemberMLM: async () => "REF-NEW",
  isCallerAdmin: async () => false,
  issueCredential: async () => "cred-new",
  joinCampaign: async () => true,
  joinOrg: async () => true,
  leaveCampaign: async () => true,
  leaveOrg: async () => true,
  linkWallet: async () => undefined,
  listActiveCampaigns: async () => [],
  listActiveOrgs: async () => [],
  listActiveProposals: async () => [
    {
      id: BigInt(1),
      status: ProposalStatus.openVote,
      title: "Adopt the IIIntl Platform Charter for Decentralized Governance",
      mechanism: VotingMechanism.simpleMajority,
      orgId: undefined,
      createdAt: BigInt(Date.now() - 5 * 86400000) * BigInt(1000000),
      tags: ["governance", "charter", "foundational"],
      description: "This proposal establishes the foundational governance charter for the IIIntl platform, defining member rights, voting protocols, and organizational structure for on-chain participatory democracy.",
      quorumPercent: BigInt(30),
      tenantId: "platform",
      proposalType: ProposalType.policy,
      votingClosesAt: BigInt(Date.now() + 5 * 86400000) * BigInt(1000000),
      updatedAt: BigInt(Date.now()) * BigInt(1000000),
      votingOpensAt: BigInt(Date.now() - 2 * 86400000) * BigInt(1000000),
      voteWindowHours: BigInt(168),
      proposer: fakePrincipal("rdmx6-jaaaa-aaaaa-aaadq-cai"),
      sponsors: [],
      sponsorThreshold: BigInt(5),
    },
    {
      id: BigInt(2),
      status: ProposalStatus.openVote,
      title: "Establish Global Environmental Action Fund",
      mechanism: VotingMechanism.supermajority66,
      orgId: undefined,
      createdAt: BigInt(Date.now() - 3 * 86400000) * BigInt(1000000),
      tags: ["environment", "funding", "climate"],
      description: "Allocate 15% of crowdfunding FSU pool to climate and environmental campaigns, establishing a dedicated Environmental Action Fund governed by member vote.",
      quorumPercent: BigInt(40),
      tenantId: "platform",
      proposalType: ProposalType.budget,
      votingClosesAt: BigInt(Date.now() + 8 * 86400000) * BigInt(1000000),
      updatedAt: BigInt(Date.now()) * BigInt(1000000),
      votingOpensAt: BigInt(Date.now() - 1 * 86400000) * BigInt(1000000),
      voteWindowHours: BigInt(240),
      proposer: fakePrincipal("rrkah-fqaaa-aaaaa-aaaaq-cai"),
      sponsors: [],
      sponsorThreshold: BigInt(5),
    },
  ],
  listAllCredentialsAdmin: async () => [],
  listAllDAOTokensAdmin: async () => [],
  listAllMemberTiers: async () => [],
  listBillingHistory: async () => [],
  listCampaigns: async () => [],
  listCampaignsByOrg: async () => [],
  listCrowdfundingCampaigns: async () => [],
  listCrowdfundingCampaignsByCategory: async () => [],
  listMyCrowdfundingCampaigns: async () => [],
  listOrgs: async () => [],
  listProposals: async () => [
    {
      id: BigInt(1),
      status: ProposalStatus.openVote,
      title: "Adopt the IIIntl Platform Charter for Decentralized Governance",
      mechanism: VotingMechanism.simpleMajority,
      orgId: undefined,
      createdAt: BigInt(Date.now() - 5 * 86400000) * BigInt(1000000),
      tags: ["governance", "charter"],
      description: "This proposal establishes the foundational governance charter for the IIIntl platform.",
      quorumPercent: BigInt(30),
      tenantId: "platform",
      proposalType: ProposalType.policy,
      votingClosesAt: BigInt(Date.now() + 5 * 86400000) * BigInt(1000000),
      updatedAt: BigInt(Date.now()) * BigInt(1000000),
      votingOpensAt: BigInt(Date.now() - 2 * 86400000) * BigInt(1000000),
      voteWindowHours: BigInt(168),
      proposer: fakePrincipal("rdmx6-jaaaa-aaaaa-aaadq-cai"),
      sponsors: [],
      sponsorThreshold: BigInt(5),
    },
  ],
  listProposalsByStatus: async () => [],
  listProposalsByType: async () => [],
  listProposalsPublic: async () => [
    {
      id: BigInt(1),
      status: ProposalStatus.openVote,
      title: "Adopt the IIIntl Platform Charter for Decentralized Governance",
      mechanism: VotingMechanism.simpleMajority,
      orgId: undefined,
      createdAt: BigInt(Date.now() - 5 * 86400000) * BigInt(1000000),
      tags: ["governance", "charter", "foundational"],
      description: "This proposal establishes the foundational governance charter for the IIIntl platform, defining member rights, voting protocols, and organizational structure.",
      quorumPercent: BigInt(30),
      tenantId: "platform",
      proposalType: ProposalType.policy,
      votingClosesAt: BigInt(Date.now() + 5 * 86400000) * BigInt(1000000),
      updatedAt: BigInt(Date.now()) * BigInt(1000000),
      votingOpensAt: BigInt(Date.now() - 2 * 86400000) * BigInt(1000000),
      voteWindowHours: BigInt(168),
      proposer: fakePrincipal("rdmx6-jaaaa-aaaaa-aaadq-cai"),
      sponsors: [],
      sponsorThreshold: BigInt(5),
    },
    {
      id: BigInt(2),
      status: ProposalStatus.openVote,
      title: "Establish Global Environmental Action Fund",
      mechanism: VotingMechanism.supermajority66,
      orgId: undefined,
      createdAt: BigInt(Date.now() - 3 * 86400000) * BigInt(1000000),
      tags: ["environment", "funding"],
      description: "Allocate 15% of crowdfunding FSU pool to climate and environmental campaigns.",
      quorumPercent: BigInt(40),
      tenantId: "platform",
      proposalType: ProposalType.budget,
      votingClosesAt: BigInt(Date.now() + 8 * 86400000) * BigInt(1000000),
      updatedAt: BigInt(Date.now()) * BigInt(1000000),
      votingOpensAt: BigInt(Date.now() - 1 * 86400000) * BigInt(1000000),
      voteWindowHours: BigInt(240),
      proposer: fakePrincipal("rrkah-fqaaa-aaaaa-aaaaq-cai"),
      sponsors: [],
      sponsorThreshold: BigInt(5),
    },
    {
      id: BigInt(3),
      status: ProposalStatus.enacted,
      title: "Launch IIINTL DAO Governance Token Airdrop",
      mechanism: VotingMechanism.simpleMajority,
      orgId: undefined,
      createdAt: BigInt(Date.now() - 14 * 86400000) * BigInt(1000000),
      tags: ["dao", "token", "airdrop"],
      description: "Authorize the genesis airdrop of IIINTL governance tokens to all active members based on membership tier.",
      quorumPercent: BigInt(35),
      tenantId: "platform",
      proposalType: ProposalType.resolution,
      votingClosesAt: BigInt(Date.now() - 4 * 86400000) * BigInt(1000000),
      enactedAt: BigInt(Date.now() - 3 * 86400000) * BigInt(1000000),
      updatedAt: BigInt(Date.now()) * BigInt(1000000),
      votingOpensAt: BigInt(Date.now() - 11 * 86400000) * BigInt(1000000),
      voteWindowHours: BigInt(168),
      proposer: fakePrincipal("rdmx6-jaaaa-aaaaa-aaadq-cai"),
      sponsors: [],
      sponsorThreshold: BigInt(5),
    },
  ],
  listPublicCredentialsByType: async () => [
    {
      id: "cred-001",
      credentialType: CredentialType.verifiedHuman,
      status: CredentialStatus.active,
      title: "Verified Human — Internet Identity",
      description: "Confirmed unique personhood verified via Internet Identity on the Internet Computer.",
      subject: fakePrincipal("rdmx6-jaaaa-aaaaa-aaadq-cai"),
      issuedBy: fakePrincipal("rrkah-fqaaa-aaaaa-aaaaq-cai"),
      metadata: "{}",
      isPublic: true,
      issuedAt: BigInt(Date.now() - 30 * 86400000) * BigInt(1000000),
      approvedAt: BigInt(Date.now() - 29 * 86400000) * BigInt(1000000),
      updatedAt: BigInt(Date.now()) * BigInt(1000000),
    },
  ],
  listRoyaltyPools: async () => [],
  listTenantMembers: async () => [],
  listTenants: async () => [],
  listThreads: async () => [],
  listThreadsByCategory: async () => [],
  listThreadsByOrg: async () => [],
  listUsers: async () => [],
  lockThread: async () => true,
  markEarningPaid: async () => true,
  markNotificationRead: async () => true,
  openProposalForVoting: async () => ({ __kind__: "ok" as const, ok: "opened" }),
  pinThread: async () => true,
  pledgeToCrowdfundingCampaign: async () => "pledge-1",
  processReferralChainBonus: async () => undefined,
  reactivateTenant: async () => true,
  recordEarning: async () => "earning-1",
  redeemFSU: async () => true,
  refundPledge: async () => true,
  registerUser: async () => "user-1",
  rejectCredential: async () => true,
  rejectCrowdfundingCampaign: async () => true,
  removeTenantMember: async () => true,
  replyToThread: async () => BigInt(1),
  resolveReferralCode: async () => null,
  revokeCredential: async () => true,
  revokeDelegation: async () => ({ __kind__: "ok" as const, ok: "revoked" }),
  runPayCycle: async () => BigInt(0),
  saveCallerUserProfile: async () => undefined,
  sendTrialExpiryNotifications: async () => "Sent 0 notifications",
  setCommissionRate: async () => true,
  setCrowdfundingConfig: async () => undefined,
  setMemberTier: async () => true,
  setPreferredLanguage: async () => undefined,
  sponsorProposal: async () => ({ __kind__: "ok" as const, ok: "sponsored" }),
  suspendTenant: async () => true,
  toggleCredentialPublic: async () => true,
  transferDAOTokens: async () => ({ __kind__: "ok" as const, ok: BigInt(100) }),
  unlinkWallet: async () => undefined,
  updateCampaign: async () => true,
  updateCrowdfundingCampaign: async () => true,
  updateOrg: async () => true,
  updateTenantBranding: async () => true,
  upgradeMemberTier: async () => true,
  withdrawSponsor: async () => ({ __kind__: "ok" as const, ok: "withdrawn" }),
};
