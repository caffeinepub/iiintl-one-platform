import { ShellPage } from "@/components/ShellPage";

export function AdminPage() {
  return (
    <ShellPage
      title="Admin Panel"
      description="Manage users, organizations, settings, reports, and platform-wide configuration."
      breadcrumb="Admin"
    />
  );
}
