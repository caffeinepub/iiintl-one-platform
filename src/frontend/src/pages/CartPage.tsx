import { ShellPage } from "@/components/ShellPage";

export function CartPage() {
  return (
    <ShellPage
      title="Cart & Orders"
      description="Review your cart, manage orders, and track purchases from IIIntl One marketplace vendors."
      breadcrumb="Commerce › Cart"
    />
  );
}
