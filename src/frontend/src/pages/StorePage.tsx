import { ShellPage } from "@/components/ShellPage";

export function StorePage() {
  return (
    <ShellPage
      title="Marketplace"
      description="Browse goods, services, and materials from vendors across the IIIntl One global network."
      breadcrumb="Commerce › Store"
    />
  );
}
